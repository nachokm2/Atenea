# Dependencias de ejecución del backend de Atenea.
# Versiones fijadas a las verificadas contra PostgreSQL 16.15 + pgvector 0.8.6
# con Python 3.12.3. No subas una versión sin volver a pasar `alembic upgrade head`
# y la suite de pruebas.

# --- API HTTP -------------------------------------------------------------
fastapi==0.115.6
uvicorn[standard]==0.32.1
python-multipart==0.0.32
slowapi==0.1.10

# --- Configuración y validación ------------------------------------------
pydantic==2.10.3
pydantic-settings==2.7.0
email-validator==2.3.0
orjson==3.12.0

# --- Base de datos (psycopg3 SÍNCRONO; prohibido async en la capa de datos)
SQLAlchemy==2.0.36
alembic==1.14.0
psycopg[binary]==3.1.18
pgvector==0.5.0

# --- Seguridad ------------------------------------------------------------
PyJWT==2.10.1
passlib[bcrypt]==1.7.4
bcrypt==4.2.1

# --- Proveedores de IA y clientes HTTP ------------------------------------
anthropic==0.117.0
httpx==0.28.1
tenacity==9.1.4

# --- Ingesta documental ---------------------------------------------------
pypdf==6.18.0
python-docx==1.2.0
markdown-it-py==4.2.0

# --- Corrección técnica determinista (sandbox SQL del worker) -------------
duckdb==1.5.5
sqlglot==30.18.0

# --- Observabilidad -------------------------------------------------------
structlog==26.1.0
