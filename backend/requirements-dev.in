# Dependencias de desarrollo y pruebas del backend de Atenea.
# Se editan aquí las directas; `requirements-dev.txt` lo genera pip-compile.
-r requirements.in

# --- Pruebas --------------------------------------------------------------
pytest==9.1.1
pytest-asyncio==1.4.0

# --- Calidad de código ----------------------------------------------------
ruff==0.14.4

# --- Tipado estático (opcional; no bloquea la CI del MVP) -----------------
mypy==1.18.2
